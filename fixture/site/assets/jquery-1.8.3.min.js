/*! jQuery v1.8.3 jquery.com | jquery.org/license */
/* Fixture stub. Only the version banner and filename matter -- they are what
   retire.js fingerprints. jQuery 1.8.3 carries known XSS CVEs, which is
   exactly the DEP-01 finding this file is here to trigger. */
(function (global) {
  var jQuery = function (sel) { return new jQuery.fn.init(sel); };
  jQuery.fn = jQuery.prototype = { jquery: "1.8.3", init: function () { return this; } };
  jQuery.fn.init.prototype = jQuery.fn;
  global.jQuery = global.$ = jQuery;
})(window);
