# Sourcemaps

| Status | Map URL | Referenced by |
|---|---|---|
| **EXPOSED (200)** | `http://localhost:8080/assets/app.bundle.js.map` (2 original sources) | `mirror-raw/localhost:8080/assets/app.bundle.js:45` |

References found: 1. Publicly reachable: **1**. Inline: 0.

Downloaded copies are in `sourcemaps/` (gitignored -- they contain our original source).
